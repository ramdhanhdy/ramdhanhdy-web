# Implementation Plans

Generated by the `improve` skill on 2026-07-14 at commit `4e7fdf8`. Execute in the order below unless dependencies say otherwise. Each executor must read its plan fully, honor its STOP conditions, run every verification gate, and update its status row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Restore a clean, trustworthy lint gate | P1 | S | — | DONE — approved in `codex/001-restore-clean-lint-gate` (`7ec024e`) |
| 002 | Add automated browser smoke verification | P1 | M | 001 | DONE — approved in `codex/002-add-browser-smoke-verification` (`a8599ab`) |
| 003 | Upgrade Vite beyond affected Windows dev-server releases | P1 | S | 001, 002 | BLOCKED — patched Vite 8.0.16 and 8.1.4 both collapse individual MDX chunks |
| 004 | Replace placeholder contact destinations | P1 | S | 002, owner-provided destinations | TODO |
| 005 | Render MDX calendar dates identically in every time zone | P2 | S | 002 | TODO |
| 006 | Make curtain navigation re-entrant and route-readiness aware | P1 | M | 001, 002 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 comes first because every later executor needs a zero-error static verification gate.
- 002 follows 001 and creates stable browser coverage before dependency or interaction changes.
- Plan 002 keeps native mobile swipe and stationary tap in the mandatory manual checklist. Its automated mobile case covers compact layout and horizontal-overflow regression only because the tested Chromium runner could neither reach the carousel's `touchmove` listener nor hit-test an interactive 3D card center.
- 003 uses the full verification command from 002 to validate the Vite/MDX/Tailwind/React build pipeline after the security update.
- Plan 003 is blocked pending a separate Vite/Rolldown code-splitting investigation. The Vite security fix cannot land while it turns individual lazy MDX chunks into one content bundle.
- 004 requires exact public contact destinations from the portfolio owner; executors must not infer or invent them.
- 005 adds its timezone regression to the browser suite created by 002.
- 006 depends on 002 because the curtain is the highest-risk motion system and must receive failing regression cases before implementation.

Recommended execution sequence: **001 → 002 → 003 → 004 → 005 → 006**. Plans 003, 004, and 005 may run independently after 002 if separate branches/worktrees are used; merge conflicts in `package.json`, `package-lock.json`, `tests/e2e/portfolio.spec.js`, or this index must be resolved deliberately.

## Findings considered and rejected

- React Router audit advisories affecting turbo-stream deserialization, `__manifest`, and mutation document requests: rejected for this quick audit because the repository is a client-only `BrowserRouter` SPA with no React Router framework/server endpoints. Re-evaluate if server rendering or framework mode is introduced.
- Moderate PostCSS stringify advisory: not planned in quick mode because no reachable application path was established; re-run the dependency audit after Plan 003 and reassess if it remains in a production/build path.
- Overview3D keyboard accessibility: valid follow-up candidate, but outside the six findings selected from the quick correctness/security/test audit. Audit accessibility separately before changing the rAF-owned card structure.
- Route-chunk rejection/error-boundary handling: distinct from slow pending chunks and deliberately excluded from Plan 006. Audit and plan separately if failed dynamic imports need a recoverable UI.

## Verification baseline at planning time

- `npm run build`: PASS (Vite 8.0.8, 457 modules transformed).
- `npm run lint`: FAIL (three errors: JSX `motion` usage and two documented memoized lazy-component sites).
- `npm audit --omit=dev --audit-level=high`: FAIL (Vite and React Router advisories; only the reachable Vite development-server risk was planned).
- Automated tests: none.
- Working tree before plan creation: clean.
